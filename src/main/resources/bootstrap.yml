spring:
  application:
    name: eportal-eventstore

---
spring:
  profiles:
      active: local
  cloud:
    config:
      enabled: true
    bus:
      enabled: false
      amqp:
        enabled: false
